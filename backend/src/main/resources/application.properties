spring.profiles.active=${SPRING_PROFILES_ACTIVE:local}
server.port=${PORT:8080}

spring.datasource.url=${DB_URL:jdbc:mysql://localhost:3306/psychometric_db?createDatabaseIfNotExist=true}
spring.datasource.username=${DB_USERNAME:springstudent}
spring.datasource.password=${DB_PASSWORD:springstudent}

jwt.secret=${JWT_SECRET:2foqAEMlzMlNEwux/M+hHvvCw8DQJI3A1bnJVJ8K1FA=}

spring.jpa.hibernate.ddl-auto=update
spring.jpa.open-in-view=false
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.default_batch_fetch_size=20
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
logging.level.org.hibernate.SQL=DEBUG

spring.data.redis.host=${REDIS_HOST:localhost}
spring.data.redis.port=${REDIS_PORT:6379}

spring.mail.host=${MAIL_HOST:smtp.gmail.com}
spring.mail.port=${MAIL_PORT:587}
spring.mail.username=${MAIL_USERNAME:omarharaz553@gmail.com}
spring.mail.password=${MAIL_PASSWORD:dtcmfznhiiotjles}

spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.default-encoding=UTF-8

cors.allowed-origins=${CORS_ALLOWED_ORIGINS:http://127.0.0.1:5500,http://localhost:5500,http://localhost:3000,http://localhost:5173,http://localhost:8080,http://localhost:8081,http://localhost:8085}

spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=50MB
spring.servlet.multipart.max-request-size=50MB

# Cloudinary CDN Configuration
cloudinary.cloud-name=${CLOUDINARY_CLOUD_NAME:n8ebougz}
cloudinary.api-key=${CLOUDINARY_API_KEY:811295576953758}
cloudinary.api-secret=${CLOUDINARY_API_SECRET:UCkrZ7QOJntAUNflVQbxnOwo4Fo}
cloudinary.folder=${CLOUDINARY_FOLDER:psychometric}

# Scoring Validity Metrics & Penalty Configuration
psychometric.scoring.validity.central-tendency-cutoff-pct=${CENTRAL_TENDENCY_CUTOFF_PCT:45.0}
psychometric.scoring.validity.sd-penalty-threshold-pct=${SD_PENALTY_THRESHOLD_PCT:60.0}
psychometric.scoring.validity.sd-penalty-rate=${SD_PENALTY_RATE:0.15}
psychometric.scoring.validity.ct-penalty-threshold-pct=${CT_PENALTY_THRESHOLD_PCT:45.0}
psychometric.scoring.validity.ct-penalty-rate=${CT_PENALTY_RATE:0.15}
psychometric.scoring.validity.max-penalty-cap-pct=${MAX_PENALTY_CAP_PCT:15.0}

logging.level.org.springframework.security=DEBUG

# Gemini AI API Configuration
gemini.api.url=${GEMINI_API_URL:https://generativelanguage.googleapis.com/v1beta/models/gemini-2.0-flash:generateContent}
gemini.api.key=${GEMINI_API_KEY:}


